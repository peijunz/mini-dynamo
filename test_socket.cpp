#include "gtstore.hpp"

int main() {
    /*GTStoreClient client;
    client.init(3);
    client.init(4);
    client.get("key_0");
    */
    GTStoreStorage storage;
    storage.init();
    return 0;
}